# LiveKit Voice Agent dependencies
# Core SDK
livekit-agents==1.4.4

# Voice Activity Detection
livekit-plugins-silero>=1.0.0

# Speech-to-Text (cloud)
livekit-plugins-deepgram>=1.0.0

# Speech-to-Text (local) — optional
# faster-whisper>=1.0.0

# Text-to-Speech (cloud) — pick one or both
livekit-plugins-cartesia>=1.0.0
livekit-plugins-elevenlabs>=1.0.0

# LLM (OpenAI-compatible)
livekit-plugins-openai>=1.0.0

# HTTP backend adapter
httpx>=0.27.0

# Backend API server
fastapi>=0.115.0
uvicorn>=0.32.0
